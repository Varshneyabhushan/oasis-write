# TODO

Open implementation notes and follow-up items for Oasis Write.

## Settings & UI

- [ ] Add line number display as a configurable setting.
- [x] Make the side panel resizable.
- [ ] Add copy buttons to code snippets.
- [ ] add status bar in the bottom 

## Markdown Editing

- [ ] Handle table insertion in markdown properly.
- [ ] Add a read-only mode for markdown files (view without accidental edits or auto-save).
- [ ] Support YAML front matter (like Claude Code skills) — parse, render, and edit it as a distinct block.

## File Links & Navigation

- [ ] Fix Windows handling for anchor links that interlink markdown files.
- [ ] Support inline references that pull in a section from another file (transclusion).

## LLM Integration

- [ ] Add IDE-style LLM support in the editor (inline assistance over the current file/selection).

## Revision Tracking

- [ ] Add a Git tree view for tracking file revisions.
